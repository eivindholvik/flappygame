function sum() {
  console.log(5 + 2);
}
"eivind";
sum("eivind");

const id = 5;

for (let i = 0; i < something.length; i++) {
  document.querySelector('nav').innerHTML += `<a href="soemthing.html?id=${i}" >go to ${i} </a>`
}

await fetch(url, {
  headers: {
    "Content-Type": "application/json",
    "autherizaton": `Bearer ${localStorage.get("token")}`,
  }
})
